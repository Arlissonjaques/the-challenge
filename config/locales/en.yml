# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#

en:
  success:
    controllers:
      auth:
        send_confirm_email: 'Confirmation email sent successfully'
        reset_password_email_sent: 'Password reset email sent'
        valid_token: ''
        session_created: 'Successful login'
        user_created: 'User created successfully'
        user_destroyed: ''
        updated_password: 'Password successfully updated'

  errors:
    models:
      user:
        format_password: 'must be at least 8 characters long including letters, numbers and some special character: [!#@%$]'
        format_email: 'needs to be in the correct format such as: example@email.com'

    controllers:
      auth:
        insufficient_params: 'Insufficient parameters'
        token_not_created: 'Failed to try to login. Try again later'
        invalid_credentials: 'Invalid email or password'
        unconfirmed_email: 'Before you must confirm your email'
        invalid_token: 'Invalid_token'
        expired_token: 'Expired token'
        different_passwords: 'Password and confirm password must be the same'
        unauthenticated: 'You are not authenticated. Please login to access this feature'
        invalid_email: 'No user found with this email'
        failed_send_confirmation_email:  'Failed to send email confirmation email'
        email_already_confirmed: 'Email already confirmed' 
